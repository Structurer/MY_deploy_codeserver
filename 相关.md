// 1. prepare 创建预编译语句
// 2. bind 按顺序填充 ? 占位符
// 3. run/all/first/raw 真正执行，必须加 await
const res = await env.DB
  .prepare(`INSERT INTO words(word, meanings) VALUES (?, ?) ON CONFLICT(word) DO UPDATE SET meanings=?`)
  .bind(word, meaningsJson, meaningsJson)
  .run()



  JS 假值：null、undefined、false、0、""、NaN。




  Promise 的 3 种状态
pending 等待中：正在处理，还没结果（外卖在路上）
fulfilled 成功：做完了，可以拿到结果（外卖送到）
rejected 失败：出错了（外卖打翻、商家取消）
状态一旦变成功 / 失败，就固定，不会再改。
